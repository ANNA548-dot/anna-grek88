// 1. Создаём переменные и инициализируем их тестовыми значениями
let user = 'netologist'; // имя текущего пользователя (можно менять)
let orderOwner = 'leo_tolstoy'; // имя владельца заказа (можно менять)
 
// 2. Проверяем, разрешено ли редактирование
if (user === 'admin' || user === orderOwner) {
    console.log('Редактирование разрешено');
} else {
    console.log('Заказ нельзя редактировать');
}
